/** @type {import('next').NextConfig} */
const nextConfig = {
  /* config options here */
  images: {
    domains: [
      "images.unsplash.com",
      "cdn.pixabay.com",
      "pixabay.com",
      "images.pexels.com",
    ],
  },
};

export default nextConfig;
